# 変更履歴

このファイルの形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [0.1.0] - 2026-08-20

最初のリリース。Google マップのタイムライン履歴をブラウザ内だけで解析し、
移動をタイムライン再生できるところまで。

### 追加

**取り込み**

- 74MB の `location-history.json` を Web Worker でストリーミング解析
- `rawSignals`（Wi-Fi の MAC アドレスを含む）は件数を数えるだけで保持しない
- 座標・時刻の正規化、同時刻・別座標の点の丸め（実データで 5,352 点）
- 30 分の空白で軌跡を分割（実データで 9,620 本）
- 長距離移動（200km 超、または 200km/h 超かつ 100km 超）は大圏コースで補間してから
  分割することで、飛行機や離島への往復が途切れないようにした
- 訪問を場所へ集約（`hierarchyLevel` 1 は二重計上を避けるため滞在時間から除外）
- 年ごとの記録カバレッジを算出
- 解析結果は IndexedDB に保存し、同じファイルは再解析しない
  （ファイル識別は先頭・末尾 256KB のハッシュで、全体は読み直さない）

**タイムライン再生**

- 期間を選んで移動を早送り再生
- 移動痕はグラデーション（彗星の尾）／実線（消えずに残る）／両方から選択
- 尾の長さは 1時間〜全期間の 10 段階。期間に応じて自動選択
- 再生速度は 1分/秒 〜 1週/秒。期間に応じて自動選択
- 記録の無い期間を飛ばす「空白スキップ」
- 現在地マーカー（移動中／停止中で表示が変わる）と滞在円
- カメラの追従モード（現在地を画面の中央に捉え続ける。自分で地図を動かすと自動で解除）
- 色分け: 単色 / 交通手段 / 年 / 速度 / 時間帯
- 2 つまみの期間スライダーに、年ごとの記録の濃さを示すデータ品質バンドを重ねた
- 時刻はすべて記録側のタイムゾーンで表示

**地図**

- 10 種類の基図（CARTO / OpenFreeMap / VersaTiles、いずれも API キー不要）
- 「なし（通信ゼロ）」を選ぶと地図タイルを一切取得しない
- 地図だけを暗くして軌跡を目立たせる「地図を沈める」スライダー

**その他**

- 統計パネル（概要・場所ランキング・年ごとの記録の濃さ）
- CI で typecheck / test / build

### 既知の制限

- 2024 年秋より前の期間には Google が生成した訪問データが無い。
  `timelinePath` からの滞在復元は未実装のため、この期間は軌跡のみ表示される
- 記録の濃さが年により大きく異なる（2019 年は約 6.2 時間/日、2025 年は約 15.5 時間/日）。
  滞在時間を年をまたいで単純比較してはいけない
- 重力マップ（滞在の重みの可視化）は未実装
- 場所の名前は表示されない。データに placeId と座標しか含まれないため
